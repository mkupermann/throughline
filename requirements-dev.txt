# Development dependencies — not needed for runtime use

pytest>=8.0
pytest-cov>=5.0

# Packaging tests build wheels without an isolated build environment.
setuptools>=68
wheel

# Release gates
black>=24.0
ruff>=0.4
